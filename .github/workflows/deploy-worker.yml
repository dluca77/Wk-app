name: Deploy Cloudflare Worker

on:
  push:
    branches: [main]
    paths:
      - 'worker/**'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sanitize Cloudflare secrets
        id: cf
        env:
          RAW_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          RAW_ACCOUNT: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          CLEAN_TOKEN=$(printf '%s' "$RAW_TOKEN" | tr -d '[:space:]')
          CLEAN_ACCOUNT=$(printf '%s' "$RAW_ACCOUNT" | tr -d '[:space:]')
          echo "::add-mask::$CLEAN_TOKEN"
          echo "::add-mask::$CLEAN_ACCOUNT"
          echo "token=$CLEAN_TOKEN" >> "$GITHUB_OUTPUT"
          echo "account=$CLEAN_ACCOUNT" >> "$GITHUB_OUTPUT"
      - name: Deploy to Cloudflare
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ steps.cf.outputs.token }}
          accountId: ${{ steps.cf.outputs.account }}
          workingDirectory: worker
      - name: DEBUG probe alternatieve ECL-slugs
        run: |
          UA="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0 Safari/537.36"
          for SLUG in "europe/conference-league" "europe/uefa-europa-conference-league" "europe/europa-conference-league-qualification" "europe/europa-conference-league-qual"; do
            echo "=== $SLUG ==="
            curl -sS -A "$UA" -L -w 'FINAL_URL:%{url_effective}\nHTTP_STATUS:%{http_code}\n' \
              "https://www.betexplorer.com/football/$SLUG/fixtures/" -o /tmp/probe.html
            grep -oc 'table-main__datetime' /tmp/probe.html || echo 0
          done
          echo "=== zoek link op homepage ==="
          curl -sS -A "$UA" "https://www.betexplorer.com/football/" -o /tmp/home.html
          grep -o 'href="/football/europe/[a-z0-9-]*conference[a-z0-9-]*/"' /tmp/home.html | sort -u
      - name: Ververs live data
        env:
          RAW_SECRET: ${{ secrets.WORKER_REFRESH_SECRET }}
        run: |
          CLEAN_SECRET=$(printf '%s' "$RAW_SECRET" | tr -d '[:space:]')
          curl -sS "https://wk2026-proxy.isaak-elia.workers.dev/refresh?key=$CLEAN_SECRET"
